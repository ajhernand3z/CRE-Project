# Minimum versions rather than exact pins so pip can pick builds compatible
# with the running Python (e.g. SQLAlchemy < 2.0.43 breaks on Python 3.14).
fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0.43
jinja2>=3.1
python-multipart>=0.0.20
httpx>=0.28
anthropic>=0.43
pytest>=8.3
